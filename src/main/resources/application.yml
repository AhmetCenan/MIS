spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mis
    username: postgres
    password: P@ssword1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  security:
    secret: miSecret@@$$
